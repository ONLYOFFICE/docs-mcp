version: '3'

services:
    docs-mcp:
        image: onlyoffice/docs-mcp
        environment:
            DOCUMENT_SERVER_BASE_URL: "https://your-onlyoffice-instance.example.com"
            DOCUMENT_SERVER_JWT_SECRET: "your-secret"
            FILE_URL_ALLOWED_ORIGINS: ""
            HTTP_ALLOWED_HOSTS: "localhost,127.0.0.1"
            HTTP_TRUST_PROXY: "1"
        ports:
          - '3001:3001'
